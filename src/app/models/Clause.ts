export interface Clause {
    id?: string,
    name?: string,
    code?: string,
    description?: string,
    // shortDescription?: string,
    heading?: string,
    type?: string,
    subclassCode?: string
}